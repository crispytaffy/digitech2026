# GeoJSONをWebマップとして公開してみよう 🌐🗺️

Google Colabで作成した `mapdata.geojson`
を使って、Webブラウザで見られるマップを作成します。

ここからは **GitHubアカウントを持っている方向けの発展編** です。

基本編では、Pythonの **Folium**
を使ってGeoJSONが正しく地図に表示できることを確認しました。\
発展編では、同じGeoJSONを **HTML + Leaflet** で表示し、**GitHub Pages**
を使ってWeb上に公開します。

``` text
Google Colab → mapdata.geojson → HTML + Leaflet → GitHub Pages → Webで公開！
```

> **ポイント**\
> FoliumとLeafletで使うのは、どちらも同じ `mapdata.geojson`
> です。GeoJSONは特定のアプリ専用ではなく、さまざまな地図ツールから利用できます。

------------------------------------------------------------------------

## 1. ColabからGeoJSONをダウンロードする

Google ColabでGeoJSONへの変換とFoliumでの表示まで完了したら、左側の
**Files** を開きます。

作成された `mapdata.geojson` を探し、ファイル名の右側にあるメニューから
**Download** を選び、PCに保存します。

このファイルには、緯度・経度が `geometry`、場所の名前や住所などが
`properties` として保存されています。

------------------------------------------------------------------------

## 2. Webマップ用のファイルを準備する

教材にある `map` フォルダを使います。

``` text
map/
├── index.html
└── mapdata.geojson
```

教材の `map/mapdata.geojson` を、Colabからダウンロードした自分の
`mapdata.geojson` に置き換えます。

-   `index.html`：Webページと地図の表示方法
-   `mapdata.geojson`：地図に表示するデータ

つまり、**データはGeoJSON、見せ方はHTML + Leaflet**
という役割分担になっています。

------------------------------------------------------------------------

## 3. GitHubに新しいリポジトリを作る

GitHubにログインし、画面右上の **「＋」→「New repository」**
を選びます。

**Repository name** には好きな名前を付けます。

例：

``` text
my-yamaguchi-map
```

この教材ではWebページとして公開するため、リポジトリは **Public**
にします。

準備ができたら **Create repository** をクリックします。

> GitHub
> Pagesで公開したWebページやファイルは、インターネット上からアクセスできる状態になります。個人情報や公開してはいけない情報が含まれていないか、アップロード前に確認しましょう。

------------------------------------------------------------------------

## 4. `map` フォルダをGitHubにアップロードする

作成したリポジトリに、次の2つのファイルが入った `map`
フォルダをアップロードします。

``` text
map/
├── index.html
└── mapdata.geojson
```

GitHubのリポジトリ画面で **Add file → Upload files**
を選び、ファイルをアップロードします。

アップロード後、**Commit changes** をクリックします。

GitHub上で次のようになっていればOKです。

``` text
リポジトリ
└── map/
    ├── index.html
    └── mapdata.geojson
```

------------------------------------------------------------------------

## 5. GitHub Pagesを有効にする

リポジトリ上部の **Settings** を開き、左側のメニューから **Pages**
を選びます。

**Build and deployment** を次のように設定します。

-   **Source**：`Deploy from a branch`
-   **Branch**：`main`
-   **Folder**：`/(root)`

設定したら **Save** をクリックします。

------------------------------------------------------------------------

## 6. 公開されたマップを確認する

公開が完了すると、**Settings → Pages** に公開先のURLが表示されます。

GitHubユーザー名が `yourname`、リポジトリ名が `my-yamaguchi-map`
の場合、公開URLは基本的に次の形です。

``` text
https://yourname.github.io/my-yamaguchi-map/
```

今回は `map` フォルダの中に `index.html`
があるため、マップは次のURLで表示できます。

``` text
https://yourname.github.io/my-yamaguchi-map/map/
```

ブラウザで開いて、Colabで確認したデータが地図に表示されれば成功です！ 🎉

> GitHub
> Pagesの公開や更新には少し時間がかかることがあります。すぐに表示されない場合は、少し待ってから再読み込みしてみましょう。

------------------------------------------------------------------------

## 7. Webマップの仕組みを見てみよう

Webマップは、大きく次の3つの要素でできています。

``` text
背景地図 ＋ GeoJSON（データ） ＋ Leaflet（地図として表示する仕組み）
```

今回の `index.html` では、JavaScriptの `fetch()` を使って
`mapdata.geojson` を読み込み、Leafletで地図上に表示しています。

Colabでは **Python + Folium** からGeoJSONを表示しましたが、ここでは
**HTML + JavaScript + Leaflet** から同じGeoJSONを表示しています。

------------------------------------------------------------------------

## 8. HTMLをカスタマイズしてみよう

Webページとして公開できたら、`index.html` を編集してみましょう。

たとえば、

-   マップのタイトルを変える
-   説明文を追加する
-   ピンの色やアイコンを変える
-   ピンをクリックしたときの情報を増やす
-   データの内容によって表示方法を変える
-   ページのデザインを変える

といったカスタマイズができます。

GitHub上で `map/index.html` を開き、**Edit this file（鉛筆マーク）**
から直接編集できます。

編集したら **Commit changes** で変更を保存します。GitHub
Pagesにも変更が反映されます。

------------------------------------------------------------------------

## 9. 生成AIに相談しながらカスタマイズする

HTMLやJavaScriptが初めてでも、生成AIに相談しながら編集できます。

まず、現在正常に動いている `index.html`
のコードを生成AIに渡し、そのうえで **変更したいことを1つ具体的に伝える**
のがおすすめです。

### プロンプト例

> Leafletを使ってGeoJSONを表示するWebマップを作っています。\
> 現在正常に動いている `index.html` は以下です。
>
> （ここにindex.htmlのコードを貼る）
>
> `mapdata.geojson`
> の「車椅子使用者用トイレ有無」のデータを使って、車椅子対応の場所は緑、それ以外は赤のピンにしたいです。\
> 初心者にも分かるように、変更する部分を教えてください。

一度にたくさん変更するのではなく、

**1つ変更する → 動かす → 確認する → 次を変更する**

と進めると、エラーが起きたときにも原因を見つけやすくなります。

------------------------------------------------------------------------

## 10. GeoJSONを更新したいとき

元のExcel / CSVを変更して新しいGeoJSONを作成した場合は、Colabでもう一度
`mapdata.geojson` を作成します。

新しいファイルをダウンロードし、GitHub上の `map/mapdata.geojson`
を更新します。

`index.html`
の仕組みを変えていなければ、同じHTMLから新しいGeoJSONを読み込んで表示できます。

これが、**データ（GeoJSON）と見せ方（HTML +
Leaflet）を分けているメリット**です。

------------------------------------------------------------------------

## うまく表示されないとき

### 404と表示される

`map` フォルダに `index.html` を置いている場合は、URLの最後に `/map/`
が必要です。

``` text
https://ユーザー名.github.io/リポジトリ名/map/
```

また、**Settings → Pages** で公開処理が完了しているか確認してください。

### 地図は出るけれどピンが表示されない

次の点を確認します。

-   `index.html` と `mapdata.geojson` が同じ `map` フォルダにあるか
-   ファイル名が `mapdata.geojson` になっているか
-   ColabのFoliumでは同じGeoJSONが正しく表示できていたか

Foliumで正しく表示できていた場合は、GeoJSONそのものではなく、HTML側の読み込み方法やファイルの配置を確認してみましょう。

### 更新したのに表示が変わらない

GitHub
Pagesへの反映には少し時間がかかる場合があります。少し待ってからブラウザを再読み込みしてください。

### エラーの原因がわからない

ブラウザの開発者ツールにある **Console**
を確認すると、エラーメッセージが表示されていることがあります。

エラーメッセージと `index.html`
のコードを生成AIに渡して相談することもできます。

------------------------------------------------------------------------

## 公開するときの注意

GitHub
Pagesで公開したWebページやGeoJSONは、インターネット上から閲覧できる状態になります。

-   個人情報を含めない
-   APIキーやパスワードを含めない
-   利用しているオープンデータのライセンスを確認する
-   必要な出典を表示する
-   地図や画像など、外部サービス・素材の利用条件を確認する

といった点に注意しましょう。

------------------------------------------------------------------------

## 公開までの流れ

**① ColabでGeoJSONを作成する**\
↓\
**② Foliumで正しく表示できるか確認する**\
↓\
**③ `mapdata.geojson` をダウンロードする**\
↓\
**④ `index.html` と組み合わせる**\
↓\
**⑤ GitHubにアップロードする**\
↓\
**⑥ GitHub Pagesを有効にする**\
↓\
**⑦ Webブラウザで確認する**\
↓\
**⑧ HTML + Leafletをカスタマイズする**

------------------------------------------------------------------------

## 同じデータを、違う方法で地図にしてみよう！

基本編では、

**GeoJSON → Python + Folium**

で地図を表示しました。

発展編では、

**GeoJSON → HTML + JavaScript + Leaflet**

で地図を表示しています。

表示する方法が変わっても、中心にあるデータは同じ `mapdata.geojson`
です。

公開したマップを誰に使ってほしいか、どんな情報を見せると便利かを考えながら、自分だけのWebマップにカスタマイズしてみましょう。
