# MAPをカスタマイズしてみよう 🗺️

基本のMAPが完成したら、データや表示方法を変えて、自分だけのMAPにカスタマイズしてみましょう。

このページでは、Google Colabで作成した **FoliumのMAP**
をカスタマイズする方法を紹介します。

解説では[宇部市「公衆トイレ一覧」](https://yamaguchi-opendata.jp/ckan/dataset/352021_ubetoile)データを使用しています。

> 💡 **ポイント**\
> 最初から全部を実装する必要はありません。\
> 「こんなMAPにしたい！」というアイデアを1つ選んで、少しずつ試してみましょう。

------------------------------------------------------------------------

## 1. まずはデータの中身を確認しよう

MAPをカスタマイズする前に、どんなデータが入っているか確認してみましょう。

``` python
print(df.columns.tolist())
```

公衆トイレのデータなら、名称、住所、設置位置、車椅子使用者用トイレ有無、乳幼児用設備設置トイレ有無、オストメイト設置トイレ有無、利用可能時間特記事項などがあります。

GeoJSONへ変換すると、`latitude` と `longitude`
は位置情報（`geometry`）になり、それ以外の列は `properties`
として残ります。元データにある情報を、あとからMAPの表示や絞り込みに利用できます。

------------------------------------------------------------------------

## 2. ピンをクリックしたときに詳細を表示する

基本のMAPでは、ピンにマウスを重ねると名称が表示されます。

``` python
tooltip=folium.GeoJsonTooltip(
    fields=["name"],
    aliases=["名称："]
)
```

ここに `popup`
を追加すると、ピンをクリックしたときに詳しい情報を表示できます。

### 例：名称・住所・設置位置を表示する

``` python
folium.GeoJson(
    geojson,
    name="公衆トイレ",
    tooltip=folium.GeoJsonTooltip(
        fields=["name"],
        aliases=["名称："]
    ),
    popup=folium.GeoJsonPopup(
        fields=["name", "address", "設置位置"],
        aliases=["名称：", "住所：", "設置位置："]
    )
).add_to(m)
```

`fields` には **GeoJSONに入っている列名**、`aliases` には
**MAP上で表示したい名前**を書きます。

### さらに情報を追加してみよう

``` python
popup=folium.GeoJsonPopup(
    fields=[
        "name",
        "address",
        "車椅子使用者用トイレ有無",
        "乳幼児用設備設置トイレ有無"
    ],
    aliases=[
        "名称：",
        "住所：",
        "車椅子対応：",
        "乳幼児設備："
    ]
)
```

> ⚠️ `fields`
> に指定する名前は、実際のデータの列名と一致している必要があります。エラーになったら
> `print(df.columns.tolist())` で確認してみましょう。

------------------------------------------------------------------------

## 3. 条件によってピンの色を変える

データの内容によって色を変えると、MAPを見ただけで違いが分かるようになります。

たとえば **車椅子対応 → 緑 / それ以外 → 青** のようにできます。

``` python
for feature in geojson["features"]:
    p = feature["properties"]
    lon, lat = feature["geometry"]["coordinates"]

    wheelchair = p.get("車椅子使用者用トイレ有無")
    color = "green" if wheelchair == "有" else "blue"

    folium.CircleMarker(
        location=[lat, lon],
        radius=7,
        color=color,
        fill=True,
        fill_color=color,
        fill_opacity=0.8,
        tooltip=p.get("name", ""),
        popup=f"""
        <b>{p.get('name', '')}</b><br>
        {p.get('address', '')}<br>
        車椅子対応：{wheelchair}
        """
    ).add_to(m)
```

データによって「有」「有り」「○」「1」など表現が異なることがあります。うまく色が変わらない場合は確認しましょう。

``` python
print(df["車椅子使用者用トイレ有無"].unique())
```

------------------------------------------------------------------------

## 4. 条件に合うデータだけ表示する

たとえば「車椅子対応の公衆トイレだけ」に絞り込む場合：

``` python
wheelchair_df = df[
    df["車椅子使用者用トイレ有無"] == "有"
]

wheelchair_df.head()
```

すでに作成した `geojson` は絞り込み前のデータなので、MAPに反映するには、

**データを絞り込む → GeoJSONに変換する → MAPに表示する**

という順番で処理します。

> 💡
> 「○○だけ表示したい」と思ったら、まず「どの列の、どの値を条件にすればよいか」を考えてみましょう。

------------------------------------------------------------------------

## 5. 表示する情報を自分のテーマに合わせる

同じ仕組みでも、使うデータによっていろいろなMAPを作れます。

  MAPのテーマ     表示すると便利そうな情報
  --------------- ------------------------------------
  カフェMAP       店名、営業時間、ジャンル、おすすめ
  観光MAP         スポット名、説明、営業時間、URL
  防災MAP         避難所名、対応災害、収容人数
  公園MAP         遊具、トイレ、駐車場
  子育てMAP       授乳、おむつ替え、利用時間
  公衆トイレMAP   車椅子対応、乳幼児設備、利用時間

「データに何が入っているか」だけでなく、**MAPを見る人にとって何が知りたい情報なのか**を考えてみましょう。

------------------------------------------------------------------------

## 6. ピンの見た目を変える

通常のピンを使いたい場合は `folium.Marker` が使えます。

``` python
folium.Marker(
    location=[latitude, longitude],
    tooltip="ここにマウスを重ねると表示",
    popup="クリックすると表示"
).add_to(m)
```

アイコンや色も変更できます。

``` python
folium.Marker(
    location=[latitude, longitude],
    tooltip="公衆トイレ",
    icon=folium.Icon(
        color="green",
        icon="info-sign"
    )
).add_to(m)
```

見た目を変えること自体が目的ではなく、**色やアイコンに意味を持たせる**と、より分かりやすいMAPになります。

------------------------------------------------------------------------

## 7. 背景地図について

この教材では、背景地図に **国土地理院「淡色地図」**を使用しています。

``` python
folium.TileLayer(
    tiles="https://cyberjapandata.gsi.go.jp/xyz/pale/{z}/{x}/{y}.png",
    attr="国土地理院",
    name="国土地理院 淡色地図"
).add_to(m)
```

背景地図と、その上に表示しているGeoJSONは別のデータです。

``` text
背景地図（国土地理院）
        ＋
自分のデータ（GeoJSON）
        ↓
      MAP
```

この関係を覚えておくと、Leafletなど別の地図ライブラリを使うときにも応用できます。

------------------------------------------------------------------------

## 8. 生成AIにカスタマイズを相談してみよう 🤖

「やりたいことは思いついたけれど、コードの書き方が分からない」というときは、生成AIに相談してみましょう。

### 例1：表示する情報を増やしたい

> Foliumで公衆トイレMAPを作っています。\
> ピンをクリックしたときに、名称と住所に加えて「車椅子使用者用トイレ有無」と「乳幼児用設備設置トイレ有無」を表示したいです。\
> どこを変更すればよいですか？

### 例2：条件によって色を変えたい

> FoliumでGeoJSONを地図に表示しています。\
> 「車椅子使用者用トイレ有無」が「有」の場所だけ、緑色で表示したいです。\
> 初心者にも分かるようにコードを教えてください。

### 例3：自分のアイデアを相談する

> 次の列があるオープンデータを使ってMAPを作っています。\
> `name`, `address`, `category`, `description`\
> このデータを使って、MAPを便利にするカスタマイズのアイデアを3つ考えてください。

### エラーが出たら

エラー文だけでなく、**何をしようとしたか・実行したコード・エラーメッセージ・データの列名**も一緒に伝えると、原因を見つけやすくなります。

> 💡
> 生成AIが提案したコードが必ず正しいとは限りません。一度に大きく変更せず、少し変更して実行し、MAPがどう変わったか確認しながら進めましょう。

------------------------------------------------------------------------

## 9. カスタマイズのアイデア

基本ができたら、こんなことにも挑戦できます。

-   特定の条件に合う場所だけ表示する
-   カテゴリーごとに色を変える
-   ピンのアイコンを変える
-   詳細情報を見やすくする
-   URLをクリックできるようにする
-   複数のデータを1つのMAPに重ねる
-   データの件数をMAP上に表示する

全部やる必要はありません。

**「このデータを、誰が、どんなときに使うMAPにしたいか？」**

を考えて、必要な機能を1つ選んでみましょう。

------------------------------------------------------------------------

# できたら共有してみよう！ 🎉

カスタマイズできたら、**どんなデータを使ったか / 誰のためのMAPか /
どこをカスタマイズしたか** を簡単に紹介してみましょう。

同じデータを使っていても、着目する情報やカスタマイズによって、まったく違うMAPになります。
