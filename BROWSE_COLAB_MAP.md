# 作成したマップをGitHub Pagesで公開する

Google Colabで作成したマップは、HTMLファイルとして保存するとWebブラウザで表示できます。

ここでは、Colabで作成した **GeoJSON（`mapdata.geojson`）** と **マップ（`index.html`）** をGitHubにアップロードし、GitHub Pagesを使ってWeb上に公開します。

> この手順は **GitHubアカウントを持っている方向けの発展編** です。

------------------------------------------------------------------------

## 1. ColabのマップをHTMLファイルに保存する

まず、Google Colabで最後に作成したマップをHTMLファイルとして保存します。

マップを表示しているセルより後に、新しいセルを追加します。
最後に表示しているマップの変数名が `m` の場合は、次のコードを実行します。

``` python
m.save("index.html")
```

カスタマイズしたマップを `m_custom` という名前で作成している場合は、次のように変数名を合わせます。

``` python
m_custom.save("index.html")
```

実行すると、Colab左側の **Files** に `index.html` が作成されます。

### ポイント

`index.html` には、Foliumで作成したマップの表示内容がHTMLとして保存されます。

そのため、Colabでピンの色やポップアップなどをカスタマイズしている場合は、**最後に完成したマップを保存**してください。

------------------------------------------------------------------------

## 2. Colabからファイルをダウンロードする

Colab左側の **Files** を確認します。

今回使用するファイルは次の2つです。

``` text
index.html
mapdata.geojson
```

それぞれファイル名の右側にあるメニューから **Download**
を選び、PCに保存します。

### 2つのファイルの役割

-   `index.html`：ブラウザで表示するマップ
-   `mapdata.geojson`：元になった位置情報・施設情報などの地図データ

> Foliumで保存した `index.html`
> には、表示に必要なGeoJSONの内容も含まれます。\
> `mapdata.geojson`
> は、元データを確認したり、今後別の地図で再利用したりできるよう、一緒にGitHubへ保存しておきます。

------------------------------------------------------------------------

## 3. GitHubに新しいリポジトリを作る

GitHubにログインし、新しいリポジトリを作成します。  

GitHubの画面右上にある **「＋」→「New repository」** を選びます。

### Repository name

好きな名前を付けます。

例：

``` text
my-yamaguchi-map
```

半角英数字とハイフンなどを使った名前がおすすめです。

### Public / Private

GitHub FreeでGitHub Pagesを利用する場合は、**Public**
を選ぶと分かりやすいです。

準備ができたら **Create repository** をクリックします。

> GitHub
> Pagesで公開したWebページは、インターネット上からアクセスできる状態になります。個人情報や公開してはいけないデータが含まれていないか、アップロード前に確認しましょう。

------------------------------------------------------------------------

## 4. `map` フォルダを作ってファイルをアップロードする

今回の教材では、リポジトリの中に `map`
フォルダを作り、その中にマップを置きます。

完成形は次のようになります。

``` text
リポジトリ
├── README.md
└── map/
    ├── index.html
    └── mapdata.geojson
```

### GitHubのWeb画面からアップロードする場合

リポジトリを開き、**Add file → Upload files** を選びます。

`map`
フォルダごとアップロードするか、あらかじめPC上で次のようなフォルダを作ってからアップロードすると分かりやすいです。

``` text
map/
├── index.html
└── mapdata.geojson
```

ファイルを追加したら、画面下部の **Commit changes** をクリックします。

これでGitHub上にファイルが保存されます。

------------------------------------------------------------------------

## 5. GitHub Pagesを有効にする

リポジトリ上部の **Settings** を開きます。

左側のメニューから **Pages** を選びます。

**Build and deployment** の設定を次のようにします。

-   **Source**：`Deploy from a branch`
-   **Branch**：`main`
-   **Folder**：`/(root)`

設定したら **Save** をクリックします。

GitHub
Pagesでは、指定したブランチとフォルダに変更が反映されると、Webサイトが公開されます。

------------------------------------------------------------------------

## 6. 公開されたページを確認する

公開が完了すると、**Settings → Pages** に公開先のURLが表示されます。

リポジトリ名が `my-yamaguchi-map`
の場合、URLは基本的に次の形になります。

``` text
https://あなたのGitHubユーザー名.github.io/my-yamaguchi-map/
```

今回は `map` フォルダの中に `index.html`
を置いているので、マップのURLは次のようになります。

``` text
https://あなたのGitHubユーザー名.github.io/my-yamaguchi-map/map/
```

このURLをWebブラウザで開いて、Colabで作成したマップが表示されれば成功です！
🎉

> GitHub
> Pagesの公開や更新には少し時間がかかることがあります。すぐに表示されない場合は、少し待ってから再読み込みしてみましょう。

------------------------------------------------------------------------

## 7. URLを誰かに共有してみよう

GitHub Pagesで公開したURLは、通常のWebページと同じように共有できます。

たとえば、

-   イベントや授業で紹介する
-   QRコードにして配布する
-   SNSで共有する
-   READMEからリンクする

といった使い方ができます。

自分のPCやGoogle
Colabを開いていなくても、URLを知っている人がブラウザからマップを見ることができます。

------------------------------------------------------------------------

## 8. マップを更新したいとき

Colabでマップをカスタマイズしたら、もう一度完成したマップを保存します。

``` python
m.save("index.html")
```

その後、新しい `index.html` をGitHubの `map`
フォルダへアップロードして、古いファイルを更新します。

`mapdata.geojson`
の内容も変更した場合は、こちらも新しいファイルに更新します。

GitHub
Pagesは、公開元のブランチに変更が反映されると自動的に再公開されます。

------------------------------------------------------------------------

## うまく表示されないとき

### 404と表示される

まず、GitHub PagesのURLを確認します。

`map` フォルダに `index.html` を置いた場合は、URLの最後に `/map/`
が必要です。

``` text
https://ユーザー名.github.io/リポジトリ名/map/
```

また、**Settings → Pages** でGitHub
Pagesの公開が完了しているか確認してください。

### Colabで見えていたマップと違う

保存したマップの変数名を確認します。

たとえば、カスタマイズ後のマップが `m_custom` なら、

``` python
m_custom.save("index.html")
```

とする必要があります。

### 更新したのに表示が変わらない

GitHub Pagesへの反映には少し時間がかかる場合があります。

少し待ってからブラウザを再読み込みしてください。それでも変わらない場合は、GitHub上の
`index.html` が新しい内容になっているか確認します。

### GitHubに公開してよいデータか確認する

GitHub
Pagesで公開したページやデータは、インターネット上から閲覧できる状態になります。

オープンデータを利用する場合も、公開元の利用規約・ライセンスを確認し、必要に応じて出典を表示しましょう。

個人情報、APIキー、パスワードなどはGitHubにアップロードしないでください。

------------------------------------------------------------------------

## 公開までの流れ

**① Colabでマップを完成させる**\
↓\
**② `index.html` として保存する**\
↓\
**③ `index.html` と `mapdata.geojson` をダウンロードする**\
↓\
**④ GitHubにアップロードする**\
↓\
**⑤ GitHub Pagesを有効にする**\
↓\
**⑥ 公開URLをブラウザで確認する**\
↓\
**⑦ URLを共有する**

------------------------------------------------------------------------

## 自分のマップをWebで公開してみよう！

ここまでできると、

**オープンデータを探す → データを整える → GeoJSONに変換する → 地図にする
→ カスタマイズする → Webで公開する**

という一連の流れを体験したことになります。

公開したマップを誰に使ってほしいか、どんな情報があると便利かも考えながら、さらにカスタマイズしてみましょう。
